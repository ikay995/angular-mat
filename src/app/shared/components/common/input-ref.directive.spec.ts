import { InputRefDirectives } from './input-ref.directive';

describe('InputRefDirective', () => {
  it('should create an instance', () => {
    const directive = new InputRefDirectives();
    expect(directive).toBeTruthy();
  });
});
